    phpmyadmin:
      image: phpmyadmin/phpmyadmin
      container_name: {{ slug }}-phpmyadmin
      restart: unless-stopped
      ports:
        - "{{ port }}:80"
      environment:
        PMA_HOST: {{ db }}
      depends_on:
        - {{ db }}